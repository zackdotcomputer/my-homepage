/**
 * Shared default export for the conversational route modules. The routes
 * exist for URL matching, metadata and status codes; the actual contents are
 * rendered by the conversation layout from the page registry (pages.tsx).
 */
export default function ConversationRoute() {
  return null;
}
